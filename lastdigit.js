var lastDigit = function (str1, str2) {
  let dig = Number(str1[str1.length - 1]);
  let pow = Number(str2[str2.length - 1]);
  return result(dig, pow);
};

function result(dig, pow) {
  if (dig == 2) {
  }
}
